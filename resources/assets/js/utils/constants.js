export const API_URL = window.baseurl;
export const globalConstants = {
  clientShareId: window.session_space_id,
  userId: window.loggin_user_id,
  userImg: window.loggin_user_image,
  isAdmin: window.is_logged_in_user_admin,
  shareName: window.current_space_name,
};
